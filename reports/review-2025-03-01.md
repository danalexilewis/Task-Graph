# Review Report — 2025-03-01 (docs vs new documentation strategy)

**Scope:** Review of `docs/` against the new documentation rule (`.cursor/rules/documentation-strategy.mdc`) plus system health. Read-only; no file edits.

---

## Code health (docs compliance)

### 1. Domain doc compliance (docs listed in `docs/domains.md`)

| Domain doc                  | Triggers (valid YAML, files, change_types, keywords, body after blank line) | Purpose (or equivalent: owns / does not own) | Decisions / gotchas (or equivalent)                 | Related projects                         | Scannable structure (tables, lists, code blocks) |
| --------------------------- | --------------------------------------------------------------------------- | -------------------------------------------- | --------------------------------------------------- | ---------------------------------------- | ------------------------------------------------ |
| **architecture.md**         | ✅ Valid                                                                    | ❌ No "Purpose" section                      | ❌ No                                               | ❌ No                                    | ✅ Tables, lists, mermaid, code                  |
| **schema.md**               | ✅ Valid                                                                    | ❌ No (only brief intro)                     | ✅ "Decisions / gotchas"                            | ✅ Non-empty                             | ✅ Tables, lists                                 |
| **cli-reference.md**        | ✅ Valid                                                                    | ❌ No                                        | ❌ No                                               | ❌ No                                    | ✅ Tables, code blocks                           |
| **cli.md**                  | ✅ Valid                                                                    | ❌ No                                        | ❌ No                                               | ❌ No                                    | ✅ Short, list-based                             |
| **cli-tables.md**           | ✅ Valid                                                                    | ❌ No (intro only)                           | ❌ No                                               | ❌ No                                    | ✅ Tables, code, list                            |
| **plan-import.md**          | ✅ Valid                                                                    | ❌ No                                        | ✅ "Gotchas / implementation notes"                 | ✅ Non-empty                             | ✅ Tables, code, lists                           |
| **plan-format.md**          | ❌ **Missing** (file starts with `# Enhanced Plan Format`)                  | ❌ No (has "Principles" only)                | ❌ No                                               | ❌ No (has "Relationship to Other Docs") | ✅ Tables, lists                                 |
| **error-handling.md**       | ✅ Valid                                                                    | ❌ No                                        | ❌ No                                               | ❌ No                                    | ⚠️ Mixed (prose + diagram + list)                |
| **testing.md**              | ✅ Valid                                                                    | ❌ No (has "Tools")                          | ✅ "Environment / gotchas"                          | ✅ Non-empty                             | ✅ Lists, code                                   |
| **infra.md**                | ✅ Valid                                                                    | ✅ "Purpose" (bullet list)                   | ✅ "Decisions / gotchas"                            | ✅ Non-empty                             | ✅ Lists, bullets                                |
| **agent-contract.md**       | ✅ Valid                                                                    | ❌ No                                        | ⚠️ "Decisions" = decision tasks, not design gotchas | ❌ No                                    | ✅ Tables, lists                                 |
| **agent-strategy.md**       | ✅ Valid                                                                    | ❌ No                                        | ❌ No                                               | ❌ No                                    | ✅ Tables, lists                                 |
| **multi-agent.md**          | ❌ **Missing** (starts with `# Multi-Agent`)                                | ✅ "What It Is" / "What It Isn't" (equiv)    | ❌ No                                               | ❌ No                                    | ✅ Tables, lists                                 |
| **mcp.md**                  | ❌ **Missing**                                                              | ❌ No                                        | ❌ No (has "Notes")                                 | ❌ No                                    | ✅ Tables, code                                  |
| **glossary.md**             | ❌ **Missing** (has `---` then `## Plan vs project`; no triggers block)     | ❌ No (has "Updating this glossary")         | N/A (reference)                                     | ❌ No                                    | ✅ Tables, lists                                 |
| **backend.md**              | ❌ **Missing** (starts with `# Backend`)                                    | ✅ "Purpose" (stub bullets)                  | ✅ "Decisions / gotchas" (stub)                     | ❌ No (stub)                             | ✅ Stub sections                                 |
| **recommended-packages.md** | ❌ **Missing**                                                              | ❌ No (has "How to use this list")           | ❌ No                                               | ❌ No                                    | ✅ Table, short list                             |

**Notes:**

- **Purpose:** Only **infra**, **multi-agent** (What It Is/Isn’t), and **backend** (stub) have an explicit owns/does-not-own style section. Others have intros or other sections but not a dedicated "Purpose" with boundaries.
- **Decisions / gotchas:** **schema**, **plan-import**, **testing**, **infra**, **backend** have a section; **agent-contract** has "Decisions" but it describes decision tasks, not design decisions or gotchas.
- **Related projects:** Only **schema**, **plan-import**, **testing**, **infra** have a non-empty "Related projects" section at the end.

### 2. Index alignment

- **domains.md → files:** All 18 domain slugs in `docs/domains.md` point to existing files. No missing or broken doc links.
- **README.md vs domains.md:**
  - **cursor-agent-cli.md** is linked in **docs/README.md** (under Agent → "Cursor Agent CLI") but **is not** in **docs/domains.md**. So it is in the categorized list but not in the domain slug table.
  - All other docs referenced in README that are domain docs appear in domains.md.
- **README vs actual doc set:** No doc in the domain list is missing from README. README does **not** link to **docs/leads/** or **docs/leads/README.md**; strategy says lead docs live in `docs/leads/` and the categorized list is in README, so the index does not currently surface the lead registry.

### 3. Gaps and suggested follow-up

**Missing triggers (required for docs in domains.md per markdown-format.mdc):**

- **plan-format.md** — Add YAML frontmatter with `triggers.files`, `triggers.change_types`, `triggers.keywords`; ensure closing `---` and blank line before `# Enhanced Plan Format`.
- **multi-agent.md** — Add full `triggers` block (e.g. files: multi-agent-related paths; change_types; keywords).
- **mcp.md** — Add `triggers` (e.g. MCP server code paths, keywords).
- **glossary.md** — Add `triggers` if it remains in domains.md (or remove from domains.md if glossary is reference-only and not used for doc-skill assignment).
- **backend.md** — Add `triggers` (stub; can use placeholder globs/keywords).
- **recommended-packages.md** — Add `triggers` (e.g. `package.json`, keywords like "dependency", "package").

**Missing Purpose (or equivalent):**

- Add a short **Purpose** section (what the doc owns, optionally what it does not own) to: **architecture**, **schema**, **cli-reference**, **cli**, **cli-tables**, **plan-import**, **plan-format**, **error-handling**, **testing**, **agent-contract**, **agent-strategy**, **mcp**, **glossary**, **recommended-packages**. **multi-agent** already has "What It Is" / "What It Isn’t" as an equivalent.

**Missing Decisions / gotchas:**

- Add a **Decisions / gotchas** section (design decisions and "why") to: **architecture**, **cli-reference**, **cli**, **cli-tables**, **plan-format**, **error-handling**, **agent-strategy**, **multi-agent**, **mcp**, **recommended-packages**. Consider renaming or splitting **agent-contract**’s "Decisions" so that "Decisions / gotchas" is reserved for design/gotchas and decision-task behavior lives under a different heading.

**Missing or empty Related projects:**

- Add **Related projects** at the end (project titles that changed this domain) to: **architecture**, **cli-reference**, **cli**, **cli-tables**, **plan-format**, **error-handling**, **agent-contract**, **agent-strategy**, **multi-agent**, **mcp**, **glossary**, **recommended-packages**. **backend** is a stub; leave empty or add when filled. **schema**, **plan-import**, **testing**, **infra** already have non-empty entries.

**Structure / prose:**

- **error-handling.md** is the main doc that leans on long prose; consider adding a short "Purpose" and a "Decisions / gotchas" section and moving some narrative into bullets or a small table where it helps scannability.

**Suggested follow-up tasks (short):**

1. Add `triggers` frontmatter to plan-format, multi-agent, mcp, glossary, backend, recommended-packages (and fix glossary so body starts with one `# Title` after the block).
2. Add Purpose (or equivalent) to all domain docs that lack it (see list above).
3. Add Decisions / gotchas to all domain docs that lack it (see list above).
4. Add Related projects to all domain docs that lack it (see list above).
5. Align index: add cursor-agent-cli to domains.md if it should be a domain slug, or leave it as reference-only and note in README; add a "Lead docs" link in README to docs/leads/ (or leads/README.md).

### 4. Skills and leads

- **docs/skills/** exists; **docs/skills/README.md** exists and is linked from **docs/README.md**. Skills are listed in the README and match the strategy (skill guides in `docs/skills/<slug>.md`).
- **docs/leads/** exists; **docs/leads/README.md** exists with a lead registry. It is **not** linked from **docs/README.md**. It is referenced from **docs/agent-strategy.md** and from **.cursor/rules/available-agents.mdc**. Adding a "Lead docs" or "Leads" entry in README that links to **docs/leads/README.md** would align with the documentation strategy.

---

## System health

### Summary

- **Task graph state:** Not observable in this run. `pnpm tg status --tasks` fails during migration with a Dolt SQL error before any task/plan data is read.
- **Gate / lint / typecheck:** Documented failure modes are conditional (OpenTUI/Bun types, minimatch, changed-files scope). Gate can also fail on test timeouts or integration setup/teardown.
- **Dependencies / tooling:** `package.json` and `pnpm-lock.yaml` are present; engines are Node ≥18, Bun ≥1. No `@types/minimatch` in `package.json`.
- **Operational readiness:** Main gap is Dolt not being usable in the environment where status was run (migration fails). When Dolt works, run `tg status --tasks` and check for stale `doing`, blocked/orphaned tasks, and plan completion.

### Task graph state

- **Command run:** `pnpm tg status --tasks` → Exit code 1. Migration failed: Dolt SQL query failed (e.g. Dolt not running, repo missing/not initialized). Stale doing, blocked/orphaned tasks, and plan completion could not be assessed. When the CLI works: run `pnpm tg status --tasks`, fix stale doing/todo per Recovery in taskgraph-workflow, run `tg export markdown --plan <id>` for completed plans.

### Gate / lint / typecheck

- Documented in `docs/infra.md` and `docs/research/cheap-gate-typecheck-lint-failures.md`. Failure modes: OpenTUI/Bun types (keep tsconfig Node-only), minimatch (no `@types/minimatch` present), context/crossplan/table scope, gate test timeouts/setup. No additional failure modes noted.

### Operational follow-up

1. **Document Dolt/migration requirement** — In `docs/infra.md`, state that all commands except init/setup require a working Dolt repo and successful migration; explain "Dolt SQL query failed" as uninitialized repo or Dolt/server not available.
2. **Optional: schema doc vs project table** — In `docs/schema.md`, note that the persisted table is `project`, `plan` is a view, and migration details live in `src/db/migrate.ts`.
3. When CLI is usable: audit task graph (stale doing, blocked/orphaned, export for completed plans).

---

## Summary and next steps

**Overall health:** Docs are only partially aligned with the new documentation strategy. Structure (tables, lists) is generally good; the main gaps are (1) missing `triggers` frontmatter on six domain docs, (2) missing Purpose / Decisions–gotchas / Related projects on most domain docs, and (3) README not surfacing lead docs. System health is limited by Dolt/migration not running in the review environment; gate and tooling are documented.

**Top actionable items:**

1. Add `triggers` frontmatter to **plan-format**, **multi-agent**, **mcp**, **glossary**, **backend**, **recommended-packages** (and fix glossary body to start with one `# Title`).
2. Add **Purpose** (owns / does not own) to domain docs that lack it (see table).
3. Add **Decisions / gotchas** and **Related projects** to domain docs that lack them.
4. Add a **Lead docs** link in **docs/README.md** to **docs/leads/README.md**.
5. In **docs/infra.md**, document that non-init/setup commands require a working Dolt repo and successful migration.

Optional: create a small plan with tasks (e.g. “Add triggers to domain docs”, “Add Purpose to domain docs”, “Add Related projects”, “Document Dolt requirement”) and assign by doc or batch.
